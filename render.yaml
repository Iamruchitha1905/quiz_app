services:
  # Truly Unified Full Stack (Dockerized)
  - type: web
    name: quiz-fullstack-docker
    runtime: docker
    plan: free
    # Since we're using Docker, we don't need build/start commands
    # The Dockerfile handles both the frontend build and backend start.
    envVars:
      - key: PORT
        value: 8000
